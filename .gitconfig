[user]
	name = Kevin von Horn
	email = name@domain.ext
[alias]
	st = status
	pick = add -p -A
  co = checkout
  ci = commit -v
	br = branch
	ffwd = pull --rebase
	changes = diff --name-status -r
	diffstat = diff --stat -r
[color]
	branch = auto
	diff = auto
	grep = auto
	interactive = auto
	ui = auto
	status = auto
	showbranch = auto
